# BeatHub Backend

This project contains the data layer for the BeatHub music streaming app.

## Setup

1. Install dependencies
npm install

2. Add your MongoDB connection string to a .env file:
MONGO_URI=your_connection_string

3. Run the seed script
node scripts/seed.js
